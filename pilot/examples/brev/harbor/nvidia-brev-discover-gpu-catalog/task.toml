schema_version = "1.3"
artifacts = []

[task]
name = "nvidia/nvidia-brev-discover-gpu-catalog"
description = "An agent asked \"what GPUs can I get on Brev?\" can locate the public page that answers it and record the URL plus three example GPU types in /app/notes/gpu-catalog.md."
authors = []
keywords = ["agent-readiness", "discover"]

[metadata]
pilot_goal_id = "discover-gpu-catalog"
readiness_dimension = "discover"
public_info_only = true
product = "nvidia-brev"

[verifier]
timeout_sec = 600.0
collect = []

[verifier.env]

[agent]
timeout_sec = 900.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
