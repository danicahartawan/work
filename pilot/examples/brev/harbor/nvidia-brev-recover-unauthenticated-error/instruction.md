When a Brev command fails for lack of authentication, an agent can diagnose the failure from the error output alone and write a recovery plan to /app/notes/recovery.md that names the documented remediation instead of retrying blindly.

You are working in a fresh Linux container with a shell.

When you are done, the following should be true:
- `/app/notes/recovery.md` exists and records your findings

Keep notes of what you tried in the files named above; the point is to show your working, not just the end state.
