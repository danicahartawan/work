schema_version = "1.3"
artifacts = []

[task]
name = "nvidia/nvidia-brev-recover-unauthenticated-error"
description = "When a Brev command fails for lack of authentication, an agent can diagnose the failure from the error output alone and write a recovery plan to /app/notes/recovery.md that names the documented remediation instead of retrying blindly."
authors = []
keywords = ["agent-readiness", "recover"]

[metadata]
pilot_goal_id = "recover-unauthenticated-error"
readiness_dimension = "recover"
public_info_only = false
product = "nvidia-brev"

[verifier]
timeout_sec = 600.0
collect = []

[verifier.env]

[agent]
timeout_sec = 900.0

[environment]
network_mode = "public"
build_timeout_sec = 600.0
os = "linux"
mcp_servers = []

[environment.env]

[solution.env]
